{
    "main": "./dist/src/index",
    "scripts": {
        "watch-end": "node clean",
        "build": "npm run watch-end && tsc",
        "watch-start": "npm run build",
        "watch-after-start": "node dist/src/index",
        "test": "npm run build && mocha dist/src/test --reporter min"
    },
    "devDependencies": {
        "@types/chai": "^4.2.12",
        "@types/mocha": "^8.0.1",
        "@types/node": "^14.11.2",
        "chai": "^4.2.0",
        "mocha": "^8.1.3"
    }
}
